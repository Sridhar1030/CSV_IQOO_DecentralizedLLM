# python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.env

# javascript / expo
node_modules/
.expo/

# model weights: never commit these, they are built by dllm/slicer.py
shards/
mac_shards/
hub_shards/
tail_shards/
mac2_shards/
mac14/
mac05/
dist*/
*.npz
*.safetensors
*.onnx
*.pte

# android build outputs
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.kotlin/
*.apk

# NPU runtime: vendor prebuilts and build output, fetched by npu/fetch_runtime.sh and built by
# npu/build_jni.sh. ~130 MB of Qualcomm/Google binaries that do not belong in git history.
android/app/src/main/jniLibs/
# Exported layer graphs, golden activations and on-device run logs: all regenerated by
# npu/export_all_layers.sh, npu/golden.py and npu/run_device.py. Hundreds of MB of binaries.
npu/out/
npu/golden*/
dist/

# LiteRT headers, fetched for the NPU JNI build rather than written here.
litert-src/
